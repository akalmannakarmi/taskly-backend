from django.urls import path
from .views import *

urlpatterns = [
    # path('create/', TaskCreateView.as_view(), name='task-create'),
    # path('update/<int:pk>/', TaskUpdateView.as_view(), name='task-update'),
    # path('start/<int:pk>/', TaskStartView.as_view(), name='task-start'),
    # path('cancel/<int:pk>/', TaskCancelView.as_view(), name='task-cancel'),
    # path('hold/<int:pk>/', TaskHoldView.as_view(), name='task-hold'),
    # path('complete/<int:pk>/', TaskCompleteView.as_view(), name='task-complete'),
    # path('delete/<int:pk>/', TaskDeleteView.as_view(), name='task-delete'),

    # path('all/', GetAllTaskView.as_view(), name='all-task'),
    # path('year/', GetYearTaskView.as_view(), name='year-task'),
    # path('month/', GetMonthTaskView.as_view(), name='month-task'),
    # path('week/', GetWeekTaskView.as_view(), name='week-task'),
    # path('day/', GetDayTaskView.as_view(), name='day-task'),
    # path('pending/', GetPendingTaskView.as_view(), name='pending-task'),
    # path('active/', GetActiveTaskView.as_view(), name='active-task'),
    # path('hold/', GetHoldTaskView.as_view(), name='hold-task'),
    # path('complete/', GetCompleteTaskView.as_view(), name='complete-task'),

    # path('detail/', TaskDetailView.as_view(), name='task-detail'),
]
